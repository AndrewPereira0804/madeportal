June 23:
Created budget page functionality, appears to function as intended, but uses the slug as the page header. should change
nav bar is a bit of a mess but its okay for now.
site still gives silly login successful popup
scheduling page seems functional but maybe not optimal display
admin page is kinda leftover and redundant, should decide how to handle that
announcement likes display weirdly, looks like author names arent displaying correctly
account page and member directory both let the user update their details. should decide where that functionality lives
