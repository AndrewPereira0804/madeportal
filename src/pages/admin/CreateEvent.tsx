//let admin CRUD an event
// can select from a list of existing Events to edit/delete
// can make a new event, set its "type" and who is allowed to see it

export default function CreateEvent(){
    return <h1> hello </h1>
}